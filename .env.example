# Copy to `.env` and fill in with your Sanity project details.
# Created by `npx sanity init` (or find them at https://sanity.io/manage).
PUBLIC_SANITY_PROJECT_ID="twh1mo0t"
PUBLIC_SANITY_DATASET="production"

# Optional — a read token, only needed if you switch the dataset to private.
# SANITY_API_READ_TOKEN=""

# Needed only to run the content migration import script (write access).
# SANITY_API_WRITE_TOKEN=""
